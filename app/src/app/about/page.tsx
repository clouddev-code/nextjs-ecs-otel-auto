export default function About() {
  return (
    <div>
      <h1>About</h1>
      <p>
        Sample App Router page used to show a second traced route (client navigation + server
        render) in the X-Ray service map alongside <code>/</code> and <code>/api/hello</code>.
      </p>
    </div>
  );
}
